package leetcode;

/**
 * 状态表示：
 * 集合：所有s(1-i)和 p（1-j）的匹配方案
 * 属性：bool值：是否存在一个合法值
 * 状态计算：
 * 分为 p[j] != *：(s[i] = p[j] || s[i] = '.')&& f(i-1,j-1)
 *
 *
 */
public class AC10 {
    public static void main(String[] args) {

    }

}
